
var IS_CONSOLE_CONTEXT = true;

//The userId of the currently logged in user
var SESSION_TOKEN;

// Scripts, page, and css resources previously loaded
var LOADED_RESOURCES = [];

function load() {

	// Get sessionToken from cookie, set to global

	// Get default role

	// if the current user has no associated role, redirect to unauthorized.html

	// else Iterate roles, get functionalities, and show associated menu

}